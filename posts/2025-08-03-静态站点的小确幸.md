# 静态站点的小确幸
部署到GitHub
---

## 🕊️ 一、准备工作

1. 登录 [GitHub](https://github.com)

2. 右上角点 **“+ → New repository”**

3. 仓库名建议起成：

   ```
   你的GitHub用户名.github.io
   ```

   比如：

   > 你的用户名是 `Extendsss`
   > 那仓库名就叫 `Extendsss.github.io`

4. 选项：

  * **Public（公开）**
  * 其他都默认

5. 点 **Create repository**

---

## 🕊️ 二、本地项目准备

你的博客目录结构应该像这样👇：

```
myblog/
│
├─ index.html
├─ css/
│   └─ style.css
├─ js/
│   └─ app.js
└─ posts/
    ├─ 2025-10-01-凌晨唱给月亮的歌-诗_爱情.md
    └─ ...
```

进入项目根目录（`index.html` 所在目录）打开命令行：

```bash
cd D:\violet\myboke
git init
git add .
git commit -m "初次提交博客"
```

---

## 🕊️ 三、连接到 GitHub 仓库

你刚创建的仓库地址会是这样：

```
https://github.com/Extendsss/Extendsss.github.io.git
```

把它加为远程仓库：

```bash
git remote add origin https://github.com/Extendsss/Extendsss.github.io.git
git branch -M main
git push -u origin main
```

---

## 🕊️ 四、如果 push 报错（常见问题）

如果出现：

```
fatal: unable to access 'https://github.com/...': Failed to connect
```

说明网络被墙或不稳定，可选解决方案👇：

### ✅ 方式一：使用代理（VPN 或 Clash）

开启代理后告诉 Git：

```bash
git config --global https.proxy http://127.0.0.1:7890
```

推送完成后可以取消：

```bash
git config --global --unset https.proxy
```

---

### ✅ 方式二：改用 SSH（推荐！）

1. 生成 SSH key：

   ```bash
   ssh-keygen -t ed25519 -C "你的邮箱@example.com"
   ```
2. 打开 `~/.ssh/id_ed25519.pub`，复制内容。
3. 去 GitHub → 右上角头像 → Settings → SSH and GPG keys → **New SSH key**
4. 粘贴进去保存。
5. 改成 SSH 方式推送：

   ```bash
   git remote set-url origin git@github.com:Extendsss/Extendsss.github.io.git
   git push -u origin main
   ```

---

## 🕊️ 五、部署 GitHub Pages

1. 打开你的仓库页面
   👉 `https://github.com/Extendsss/Extendsss.github.io`
2. 点击上方的 **Settings**
3. 左侧栏选择 **Pages**
4. 在 “Build and deployment” 里选择：

  * **Source:** Deploy from a branch
  * **Branch:** `main`
  * **Folder:** `/ (root)`
5. 等几分钟后，GitHub 自动部署。

---

## 🕊️ 六、访问你的网站 ✨

访问地址：

```
https://Extendsss.github.io
```

🎉 这就是你的静态博客！支持 HTML / JS / Markdown 渲染，一切由 GitHub 免费托管。

---


